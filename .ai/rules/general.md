---
paths:
  - '**'
---

# General

## Commit straight to main during alpha
While the project is in alpha, commit work directly to `main` — do not create a feature branch or open a PR unless asked.

Revisit this once the project leaves alpha and real users depend on `main` staying deployable.
